[package]
name = "weather-hub"
version = "0.1.0"
edition = "2021"
description = "Central weather mesh hub - aggregates data from all stations"
license = "MIT OR Apache-2.0"
publish = false

[[bin]]
name = "weather-hub"
path = "src/main.rs"

[dependencies]
weather-contracts = { path = "../weather-contracts", features = [
    "linkable",
    "migratable",
] }
aimdb-core = { path = "../../aimdb/aimdb-core", features = ["derive"] }
# `observability` makes `.observe()`'s signal gauge live; without it core hands
# back an inert handle and no signal stats surface on record.list/record.get.
aimdb-tokio-adapter = { path = "../../aimdb/aimdb-tokio-adapter", features = [
    "observability",
] }
aimdb-data-contracts = { path = "../../aimdb/aimdb-data-contracts", features = [
    "linkable",
    "observable",
    "migratable",
] }
aimdb-mqtt-connector = { path = "../../aimdb/aimdb-mqtt-connector", features = [
    "tokio-runtime",
] }
# AimX server for mesh mode: `aimdb record list --url tcp://…:7433`
# (design 042 §7). Unused in the local demo path.
aimdb-tcp-connector = { path = "../../aimdb/aimdb-tcp-connector", features = [
    "tokio-runtime",
] }

tokio = { version = "1", features = ["full"] }
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
