diff --git a/aimdb-mqtt-connector/Cargo.toml b/aimdb-mqtt-connector/Cargo.toml
index b58500e..a9bd256 100644
--- a/aimdb-mqtt-connector/Cargo.toml
+++ b/aimdb-mqtt-connector/Cargo.toml
@@ -23,6 +23,22 @@ tokio-runtime = [
     "async-stream",
     "futures-util",
 ]
+# --- TLS backend for the tokio client (`mqtts://`) ---
+#
+# Pick exactly one, or neither. Neither is a real choice rather than an
+# oversight: a deployment that speaks `mqtt://` to a broker on a trusted network
+# links no TLS stack at all, which for a shared library dropped into somebody
+# else's C++ application is the difference between inheriting a system OpenSSL
+# ABI constraint and inheriting nothing. `mqtts://` then fails at connect time
+# with a message naming the missing feature, rather than at link time.
+#
+# `tokio-native-tls` is what `weather-station`'s default has always selected, so
+# it stays the compatible choice. `tokio-rustls` is the one an FFI consumer
+# wants: no `libssl.so`, no `libcrypto.so`, and no second OpenSSL in a process
+# that already has one.
+tokio-native-tls = ["tokio-runtime", "rumqttc/use-native-tls"]
+tokio-rustls = ["tokio-runtime", "rumqttc/use-rustls", "dep:rustls-native-certs"]
+
 embassy-runtime = [
     "aimdb-core/alloc",                          # Need alloc for collect_inbound_routes
     "aimdb-core/connector-session",              # `pump_sink`/`pump_source`/`Source`/`Payload`
@@ -65,9 +81,17 @@ uuid = { version = "1.0", features = ["v4"], optional = true }
 
 # Tokio runtime dependencies (std)
 tokio = { workspace = true, optional = true, features = ["sync", "time"] }
-rumqttc = { version = "0.25", optional = true, default-features = false, features = [
-    "use-native-tls",
-] }
+# No TLS feature here: the backend is the consumer's choice, made through
+# `tokio-native-tls` or `tokio-rustls` below. rumqttc's own default is
+# `use-rustls`; `default-features = false` is what keeps that from arriving
+# behind the choice.
+rumqttc = { version = "0.25", optional = true, default-features = false }
+
+# Platform trust roots for the rustls backend. rumqttc's
+# `TlsConfiguration::default()` loads these too, but it `expect`s on failure —
+# and a panic on the connect path is what `aimdb-sync`'s FFI consumers cannot
+# afford. Loaded explicitly instead, and reported as a `DbError`.
+rustls-native-certs = { version = "0.8", optional = true }
 async-stream = { version = "0.3", optional = true }
 futures-util = { version = "0.3", optional = true, default-features = false, features = [
     "alloc",
diff --git a/aimdb-mqtt-connector/src/tokio_client.rs b/aimdb-mqtt-connector/src/tokio_client.rs
index e4b2ca0..c6966b6 100644
--- a/aimdb-mqtt-connector/src/tokio_client.rs
+++ b/aimdb-mqtt-connector/src/tokio_client.rs
@@ -177,10 +177,20 @@ impl MqttConnectorImpl {
             mqtt_opts.set_credentials(username, password);
         }
 
-        // mqtts:// selects the TLS transport (system trust roots via
-        // native-tls); rumqttc otherwise speaks plain TCP regardless of port.
+        // mqtts:// selects the TLS transport; rumqttc otherwise speaks plain
+        // TCP regardless of port. Which stack answers is a build-time choice —
+        // see `tls_configuration`.
+        //
+        // The whole branch is gated, not just the configuration: rumqttc gates
+        // `TlsConfiguration` *and* `Transport::Tls` on having a backend, so a
+        // build with neither cannot even name the types.
+        #[cfg(any(feature = "tokio-native-tls", feature = "tokio-rustls"))]
+        if connector_url.scheme == "mqtts" {
+            mqtt_opts.set_transport(rumqttc::Transport::Tls(tls_configuration()?));
+        }
+        #[cfg(not(any(feature = "tokio-native-tls", feature = "tokio-rustls")))]
         if connector_url.scheme == "mqtts" {
-            mqtt_opts.set_transport(rumqttc::Transport::Tls(rumqttc::TlsConfiguration::Native));
+            return Err(no_tls_backend());
         }
 
         // Wrap router early so we can count topics for capacity calculation
@@ -343,6 +353,58 @@ impl Source for MqttEventLoopSource {
     }
 }
 
+/// The TLS configuration for `mqtts://`, from whichever backend this build
+/// selected.
+///
+/// Three arms rather than two, because "no TLS backend" is a supported build:
+/// a deployment on a trusted network links neither stack, and only discovers
+/// the gap if it actually asks for `mqtts://`. That is a link-line decision for
+/// anyone shipping this inside a shared library, so it fails here with a
+/// message rather than at the linker with a missing symbol.
+#[cfg(feature = "tokio-native-tls")]
+fn tls_configuration() -> Result<rumqttc::TlsConfiguration, String> {
+    Ok(rumqttc::TlsConfiguration::Native)
+}
+
+#[cfg(all(feature = "tokio-rustls", not(feature = "tokio-native-tls")))]
+fn tls_configuration() -> Result<rumqttc::TlsConfiguration, String> {
+    use rumqttc::tokio_rustls::rustls::{ClientConfig, RootCertStore};
+
+    // Built by hand rather than via `TlsConfiguration::default()`, which does
+    // the same work and then `expect`s on failure. A panic here would cross an
+    // FFI boundary as undefined behaviour; a `DbError` is a status code.
+    let mut roots = RootCertStore::empty();
+    let loaded = rustls_native_certs::load_native_certs();
+    for cert in loaded.certs {
+        // A trust store with one unparseable certificate is still a trust
+        // store; refusing the lot would be worse than skipping the entry.
+        let _ = roots.add(cert);
+    }
+    if roots.is_empty() {
+        return Err("no usable platform trust roots were found, so no broker \
+                    certificate could be verified"
+            .to_string());
+    }
+
+    Ok(rumqttc::TlsConfiguration::Rustls(Arc::new(
+        ClientConfig::builder()
+            .with_root_certificates(roots)
+            .with_no_client_auth(),
+    )))
+}
+
+/// Why an `mqtts://` url cannot be honoured by a build with no TLS backend.
+///
+/// A message rather than a missing symbol: linking no TLS stack is a supported
+/// choice for a deployment on a trusted network — and for a shared library, the
+/// difference between inheriting a system OpenSSL ABI and inheriting nothing.
+#[cfg(not(any(feature = "tokio-native-tls", feature = "tokio-rustls")))]
+fn no_tls_backend() -> String {
+    "this build has no TLS backend — rebuild aimdb-mqtt-connector with the \
+     `tokio-rustls` or `tokio-native-tls` feature, or use an mqtt:// url"
+        .to_string()
+}
+
 #[cfg(test)]
 mod tests {
     use super::*;
